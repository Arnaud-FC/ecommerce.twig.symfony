{% include 'base.html.twig'  %}


{% block body %}
    <div class="container">
    <br>
        <h1>Ajouter du stock à "{{ product.name }}"</h1>
        {% include 'layouts/_flash_message.html.twig' %}
    <br>
        {{ form_start(form)}}
            {{ form_row(form.qte, {'attr':{"class":"form form-control"} } )}}
            <br>
            <input type="submit" class="btn btn-success btn-block">
        {{ form_end(form)}}
    </div>
{% endblock %}